/*#include <raylib.h>

class Boar
{
    private:
        std::unordered_map<std::string, Texture2D> texture = 
        {
            {"idle", LoadTexture("textures/Legacy-Fantasy - High Forest 2.3/Mob/")},
            {"run", LoadTexture("textures/Legacy-Fantasy - High Forest 2.3/Mob/")},
            {"attack", LoadTexture("textures/Legacy-Fantasy - High Forest 2.3/Mob/")},
            {"hit", LoadTexture("textures/Legacy-Fantasy - High Forest 2.3/Mob/")}
        };
    private:
        //animations
        float updateTime{};
        float runningTime{};
        int frame{};
        int maxFrames{};
        std::string current{};
    public:
        Boar();
        void Tick(float deltaTime);
};
*/
